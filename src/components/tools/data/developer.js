export const developerTools = [
  {
    slug: 'json-studio', title: 'JSON Formatter & Validator', shortTitle: 'JSON Formatter', icon: '{}', category: 'Developer',
    description: 'Format, validate, minify, and sort JSON online with precise syntax error messages and line numbers.',
    tags: ['json formatter', 'json validator', 'beautify', 'minify', 'pretty print'],
    intro: 'A minified API response is unreadable, and a missing comma buried three hundred lines deep is worse. This formatter pretty-prints JSON with configurable indentation, validates it with an error message that names the position of the problem, and minifies it again when you need to ship it.',
    steps: [
      'Paste your JSON into the left panel.',
      'Press Format to indent it, or Minify to strip every unnecessary byte.',
      'If it is invalid, read the error message — it names the character position of the fault.',
      'Copy the formatted result, or sort keys alphabetically to make two documents comparable.',
    ],
    faqs: [
      { q: 'Why does my JSON fail to parse?', a: 'The three usual causes are a trailing comma after the last item, single quotes instead of double quotes, and unescaped newlines inside a string. Strict JSON allows none of them, even though JavaScript object literals do.' },
      { q: 'Are comments allowed in JSON?', a: 'No. The specification has no comment syntax. Variants like JSONC and JSON5 add them, but a standard parser will reject a document containing // or /* */.' },
      { q: 'Does formatting change my data?', a: 'No. Whitespace between tokens has no meaning in JSON, so formatting and minifying are lossless. Key order is preserved unless you explicitly sort.' },
      { q: 'Is my JSON uploaded to a server?', a: 'No. Parsing uses the browser’s built-in JSON engine, so API keys, tokens, and customer data in your payload never leave the machine.' },
    ],
  },
  {
    slug: 'base64-tool', title: 'Base64 Encoder & Decoder', shortTitle: 'Base64 Tool', icon: '64', category: 'Developer',
    description: 'Encode text to Base64 or decode Base64 back to Unicode text, with URL-safe Base64 support.',
    tags: ['base64', 'encode', 'decode', 'url safe', 'binary'],
    intro: 'Base64 exists because plenty of transports — email headers, JSON strings, data URIs, HTTP basic auth — only reliably carry printable ASCII. It encodes arbitrary bytes into 64 safe characters at a cost of about 33% extra size. This tool converts in both directions and handles Unicode correctly.',
    steps: [
      'Paste text to encode, or a Base64 string to decode.',
      'Press Encode or Decode.',
      'Switch on URL-safe mode if the value goes into a query string or path.',
      'Copy the result.',
    ],
    faqs: [
      { q: 'Is Base64 encryption?', a: 'No, and treating it as such is a genuine security mistake. It is a reversible encoding with no key. Anyone can decode it instantly, so never use it to hide a secret.' },
      { q: 'What is URL-safe Base64?', a: 'A variant that replaces + with - and / with _, because the standard characters have reserved meanings in URLs. JWTs use this variant, which is why they contain no plus or slash characters.' },
      { q: 'Why does Base64 make data larger?', a: 'Because it packs three bytes into four characters, adding roughly one third. The = padding at the end aligns the output to a multiple of four characters.' },
      { q: 'Why did my emoji break in another tool?', a: 'Naive implementations call btoa directly, which only handles Latin-1. This tool encodes to UTF-8 bytes first, so emoji and non-Latin scripts round-trip correctly.' },
    ],
  },
  {
    slug: 'url-encoder', title: 'URL Encoder & Decoder', shortTitle: 'URL Encoder', icon: '%2F', category: 'Developer',
    description: 'Percent-encode text for safe use in URLs, or decode percent-encoded URLs back into readable text.',
    tags: ['url encode', 'percent encoding', 'decode', 'query string', 'uri'],
    intro: 'URLs can only carry a limited character set, so spaces become %20 and anything non-ASCII becomes a percent-escaped byte sequence. This tool encodes and decodes in both the component and full-URI modes, which differ in whether they escape reserved characters like slashes and ampersands.',
    steps: [
      'Paste the text or URL you want to convert.',
      'Choose component mode for a single query value, or full URI mode for a complete address.',
      'Press Encode or Decode.',
      'Copy the result into your code or address bar.',
    ],
    faqs: [
      { q: 'What is the difference between component and full-URI encoding?', a: 'Component encoding escapes reserved characters including / ? & = #, which is correct for a single parameter value. Full-URI encoding leaves them intact so the URL structure survives.' },
      { q: 'Why does a space sometimes become + instead of %20?', a: 'Because HTML form submissions use the application/x-www-form-urlencoded format, where + means space. In a path segment a space must be %20; both appear in the wild.' },
      { q: 'How is non-English text encoded?', a: 'It is converted to UTF-8 bytes first, then each byte is percent-escaped. That is why a single accented character often becomes two escape sequences.' },
      { q: 'What is double encoding?', a: 'Encoding an already-encoded string, turning %20 into %2520. It is a common source of broken links and usually means encoding was applied at two layers of the stack.' },
    ],
  },
  {
    slug: 'jwt-inspector', title: 'JWT Decoder & Inspector', shortTitle: 'JWT Decoder', icon: 'JWT', category: 'Developer',
    description: 'Decode JWT headers and payload claims locally, inspect standard claims, and check token expiry.',
    tags: ['jwt', 'json web token', 'decode', 'claims', 'expiry'],
    intro: 'A JSON Web Token is three Base64url segments separated by dots: a header naming the algorithm, a payload of claims, and a signature. Debugging auth problems usually means reading the payload, which this tool decodes entirely in your browser, with expiry and issued-at times rendered as readable dates.',
    steps: [
      'Paste the JWT into the input box.',
      'Read the decoded header and payload side by side.',
      'Check the exp and iat claims, which are shown as human-readable dates.',
      'Note the algorithm in the header — this tool does not verify signatures.',
    ],
    faqs: [
      { q: 'Does this verify the token signature?', a: 'No. Verification requires the signing secret or public key, which should never be pasted into a web page. Decoding shows what the token claims; only your server should decide whether to trust it.' },
      { q: 'Is it safe to paste a token here?', a: 'Decoding happens entirely in your browser with no network request. That said, a live access token is a credential — prefer expired or test tokens as a matter of habit.' },
      { q: 'What do the standard claims mean?', a: 'iss is the issuer, sub the subject or user id, aud the intended audience, exp the expiry time, iat the issue time, and nbf the time before which the token is invalid. All times are Unix seconds.' },
      { q: 'What is the "alg: none" vulnerability?', a: 'A historical flaw where a server accepted a token declaring no algorithm, letting anyone forge one. Always pin the expected algorithm server-side rather than trusting the header.' },
    ],
  },
  {
    slug: 'uuid-generator', title: 'UUID v4 Generator', shortTitle: 'UUID Generator', icon: 'ID', category: 'Developer',
    description: 'Generate random RFC 4122 version 4 UUIDs and GUIDs in bulk, with uppercase, hyphen-free, and braced formats.',
    tags: ['uuid', 'guid', 'v4', 'generator', 'identifier'],
    intro: 'A version 4 UUID is 122 random bits formatted as 36 characters, giving effectively zero chance of collision without any central coordination. That is why they are used for database keys, correlation ids, and file names. This generator uses the browser’s cryptographic random source and can produce up to 500 at a time.',
    steps: [
      'Set how many UUIDs you need.',
      'Choose a format: standard lowercase, uppercase, no hyphens, or braced GUID style.',
      'Press Generate.',
      'Copy the whole list in one click.',
    ],
    faqs: [
      { q: 'How likely is a UUID collision?', a: 'Negligible. You would need to generate about 2.7 × 10¹⁸ UUIDs to reach a one-in-a-billion chance of a single duplicate — more than any practical system will ever create.' },
      { q: 'What is the difference between a UUID and a GUID?', a: 'None, functionally. GUID is Microsoft’s name for the same 128-bit identifier, conventionally written in braces. The bytes are identical.' },
      { q: 'Should I use a UUID as a database primary key?', a: 'It depends. UUIDs avoid coordination between services but harm index locality in B-tree databases because inserts land randomly. UUID v7, which is time-ordered, exists specifically to solve that.' },
      { q: 'Are these UUIDs cryptographically random?', a: 'Yes. They come from crypto.randomUUID, backed by the platform’s CSPRNG, not from Math.random.' },
    ],
  },
  {
    slug: 'regex-tester', title: 'JavaScript Regex Tester', shortTitle: 'Regex Tester', icon: '.*', category: 'Developer',
    description: 'Test JavaScript regular expressions live with highlighted matches, capture groups, positions, and flag support.',
    tags: ['regex', 'regular expression', 'tester', 'javascript', 'pattern'],
    intro: 'Regular expressions are dense enough that trial and error is the only realistic way to build one. This tester evaluates your pattern against sample text on every keystroke, highlights each match in place, and lists capture groups and their positions so you can see exactly what the engine matched.',
    steps: [
      'Type your pattern without the surrounding slashes.',
      'Set flags: g for all matches, i for case-insensitive, m for multiline.',
      'Paste sample text underneath.',
      'Review the highlighted matches and the capture groups for each one.',
    ],
    faqs: [
      { q: 'Which regex flavour does this use?', a: 'JavaScript’s, via the native RegExp engine. Most syntax is shared with PCRE, but lookbehind support varies by browser and some PCRE features such as recursion do not exist here.' },
      { q: 'What is catastrophic backtracking?', a: 'A pattern with nested quantifiers like (a+)+ can take exponential time on non-matching input, freezing the page or hanging a server. Avoid nesting quantifiers around the same character class.' },
      { q: 'How do I match a literal dot?', a: 'Escape it as \\. — an unescaped dot matches any character except a newline, which is why unanchored patterns often match far more than intended.' },
      { q: 'Should I parse HTML with a regex?', a: 'No. HTML is not a regular language, so nesting will eventually defeat any pattern. Use a DOM parser; regex is for flat, predictable text.' },
    ],
  },
  {
    slug: 'hash-generator', title: 'SHA-256 & SHA-512 Hash Generator', shortTitle: 'Hash Generator', icon: '#', category: 'Developer',
    description: 'Generate SHA-1, SHA-256, SHA-384, and SHA-512 hashes of any text using the browser’s native Web Crypto API.',
    tags: ['hash', 'sha256', 'sha512', 'checksum', 'digest'],
    intro: 'A cryptographic hash turns any input into a fixed-length fingerprint that cannot practically be reversed and changes completely if a single bit of input changes. This generator computes SHA-1, SHA-256, SHA-384, and SHA-512 using the browser’s native Web Crypto implementation.',
    steps: [
      'Type or paste the text you want to hash.',
      'All four digests are computed at once as you type.',
      'Compare a digest against a published checksum to verify integrity.',
      'Copy any individual hash with one click.',
    ],
    faqs: [
      { q: 'Can a hash be reversed?', a: 'Not by computation. But short or common inputs can be found by brute force and rainbow tables, which is why hashing a password without a unique salt offers so little protection.' },
      { q: 'Why is MD5 not offered?', a: 'Because it is cryptographically broken — practical collisions have been demonstrated for years. Offering it invites misuse in security contexts where it must never be used.' },
      { q: 'Should I hash passwords with SHA-256?', a: 'No. Fast hashes are the wrong tool for passwords precisely because they are fast. Use bcrypt, scrypt, or Argon2, which are deliberately slow and salted.' },
      { q: 'Which SHA variant should I pick?', a: 'SHA-256 for almost everything. SHA-512 is not meaningfully more secure in practice and is faster on 64-bit hardware; SHA-1 is included only for verifying legacy checksums.' },
    ],
  },
  {
    slug: 'html-encoder', title: 'HTML Entity Encoder & Decoder', shortTitle: 'HTML Encoder', icon: '&#', category: 'Developer',
    description: 'Convert characters to HTML entities and decode entities back to plain text, protecting markup from breaking.',
    tags: ['html entities', 'escape', 'encode', 'ampersand', 'special characters'],
    intro: 'Five characters have structural meaning in HTML — less than, greater than, ampersand, and both quote marks — and any of them appearing literally in content can break the page or open an injection hole. This tool escapes them to named entities and decodes entities back to readable text.',
    steps: [
      'Paste the text or markup you want to convert.',
      'Press Encode to escape the special characters, or Decode to reverse it.',
      'Choose whether to escape non-ASCII characters as numeric entities too.',
      'Copy the safe output into your template.',
    ],
    faqs: [
      { q: 'Which characters must always be escaped?', a: '& becomes &amp;, < becomes &lt;, > becomes &gt;, " becomes &quot;, and \' becomes &#39;. Escaping the ampersand first matters, otherwise you double-escape everything else.' },
      { q: 'Do I still need entities if the page is UTF-8?', a: 'For accented letters and emoji, no — UTF-8 handles them directly. The five structural characters must still be escaped regardless of encoding.' },
      { q: 'Does escaping prevent XSS?', a: 'Context-appropriate escaping is the core defence, but the correct escaping differs inside HTML text, an attribute, a URL, and a script block. Use your framework’s contextual escaping rather than one blanket pass.' },
      { q: 'What is a non-breaking space?', a: '&nbsp; renders as a space that never wraps to a new line. Useful between a number and its unit, but strings of them used for layout indentation are a sign the CSS needs attention.' },
    ],
  },
  {
    slug: 'csv-to-json', title: 'CSV to JSON Converter', shortTitle: 'CSV to JSON', icon: 'C→J', category: 'Developer',
    description: 'Convert CSV data into a JSON array of objects, with header detection, custom delimiters, and quoted field support.',
    tags: ['csv to json', 'convert', 'parse', 'delimiter', 'data'],
    intro: 'Spreadsheets export CSV; APIs consume JSON. This converter bridges the two, parsing quoted fields containing commas and newlines correctly rather than naively splitting on the delimiter — which is where most quick scripts fall over on real-world data.',
    steps: [
      'Paste your CSV, including the header row.',
      'Confirm the delimiter — comma, semicolon, or tab.',
      'Choose whether the first row supplies the object keys.',
      'Copy the resulting JSON array.',
    ],
    faqs: [
      { q: 'How are commas inside a field handled?', a: 'Correctly, provided the field is wrapped in double quotes as the CSV convention requires. The parser tracks quote state rather than splitting blindly on every delimiter.' },
      { q: 'Why does my European CSV use semicolons?', a: 'Because locales that use a comma as the decimal separator would otherwise create ambiguity, so Excel switches the field delimiter to a semicolon. Select it in the delimiter option.' },
      { q: 'Are numbers converted to JSON numbers?', a: 'Yes when a field parses cleanly as a number, otherwise it stays a string. Leading zeros are preserved as text so postcodes and account numbers are not corrupted.' },
      { q: 'How are empty fields represented?', a: 'As empty strings, which preserves the column structure. If you need nulls, a find and replace over the output is the simplest fix.' },
    ],
  },
  {
    slug: 'json-to-csv', title: 'JSON to CSV Converter', shortTitle: 'JSON to CSV', icon: 'J→C', category: 'Developer',
    description: 'Turn a JSON array of objects into CSV, flattening nested fields and quoting values that need escaping.',
    tags: ['json to csv', 'export', 'spreadsheet', 'flatten', 'convert'],
    intro: 'Getting an API response in front of someone who lives in Excel means converting JSON to CSV. This tool collects the union of every key across the array so no column is silently dropped, flattens nested objects into dotted column names, and quotes any value containing a delimiter or newline.',
    steps: [
      'Paste a JSON array of objects.',
      'Choose your delimiter and whether nested objects should be flattened.',
      'Review the generated header row for completeness.',
      'Copy the CSV and paste it straight into a spreadsheet.',
    ],
    faqs: [
      { q: 'What happens if objects have different keys?', a: 'Every key found anywhere in the array becomes a column, and records missing that key get an empty cell. This avoids losing fields that only appear in later rows.' },
      { q: 'How are nested objects handled?', a: 'They are flattened into dotted column names, so a user object with a name field becomes a "user.name" column. Arrays are serialised as JSON inside a single cell.' },
      { q: 'Why are some values wrapped in quotes?', a: 'Because they contain a delimiter, a quote character, or a line break. Doubling internal quotes and wrapping the field is the CSV standard for escaping.' },
      { q: 'Will Excel open the file correctly?', a: 'Usually. If accented characters look wrong, the issue is Excel guessing the encoding — importing explicitly as UTF-8 rather than double-clicking the file fixes it.' },
    ],
  },
  {
    slug: 'markdown-to-html', title: 'Markdown to HTML Converter', shortTitle: 'Markdown to HTML', icon: 'MD', category: 'Developer',
    description: 'Convert Markdown to clean HTML with support for headings, lists, links, images, code blocks, tables, and quotes.',
    tags: ['markdown', 'html', 'convert', 'md', 'preview'],
    intro: 'Markdown is comfortable to write and useless to a browser, so at some point it has to become HTML. This converter handles the common Markdown constructs — headings, emphasis, links, images, fenced code, tables, blockquotes, and both list types — and shows a live preview alongside the generated markup.',
    steps: [
      'Type or paste Markdown into the editor.',
      'Watch the rendered preview update live.',
      'Switch to the HTML tab to see the generated markup.',
      'Copy the HTML into your CMS or template.',
    ],
    faqs: [
      { q: 'Which Markdown flavour is supported?', a: 'The core CommonMark constructs plus GitHub-style fenced code blocks and pipe tables. Footnotes, definition lists, and other extensions are not included.' },
      { q: 'Is raw HTML inside the Markdown preserved?', a: 'Inline HTML is escaped rather than passed through, which keeps the output safe when converting text from an untrusted source.' },
      { q: 'Why does my list not render?', a: 'Nearly always missing a blank line before the list, or inconsistent indentation. Markdown needs a blank line to end the preceding paragraph before a block starts.' },
      { q: 'How do I write a code block inside a code block?', a: 'Use more backticks on the outer fence than the inner one — four outside and three inside works reliably.' },
    ],
  },
  {
    slug: 'color-converter', title: 'HEX to RGB & HSL Color Converter', shortTitle: 'Color Converter', icon: 'RGB', category: 'Developer',
    description: 'Convert colours between HEX, RGB, HSL, and CSS formats with a live preview and contrast information.',
    tags: ['hex to rgb', 'color converter', 'hsl', 'css color', 'picker'],
    intro: 'Designers hand over hex codes, CSS animations want HSL because it interpolates sensibly, and canvas work needs RGB channels. This converter takes a colour in any of those formats and produces the others, with a live swatch and the contrast ratio against black and white.',
    steps: [
      'Enter a colour as HEX, RGB, or HSL, or pick one with the colour input.',
      'Read the equivalent values in every other format.',
      'Check the contrast ratios against black and white text.',
      'Copy the format you need in ready-to-paste CSS syntax.',
    ],
    faqs: [
      { q: 'Why use HSL instead of HEX?', a: 'Because HSL is human-readable. Making a colour lighter means raising one number rather than recalculating three hex pairs, which makes generating a consistent palette far easier.' },
      { q: 'What contrast ratio do I need for accessibility?', a: 'WCAG AA requires 4.5:1 for normal text and 3:1 for large text; AAA raises that to 7:1. The ratios shown here tell you at a glance whether black or white text works on the colour.' },
      { q: 'What does the fourth value in RGBA mean?', a: 'Alpha, the opacity, from 0 for fully transparent to 1 for fully opaque. Modern CSS also accepts an eight-digit hex where the last pair encodes alpha.' },
      { q: 'Why does the same hex look different on two screens?', a: 'Colour management. Displays vary in gamut and calibration, and without an explicit colour profile the same sRGB value renders differently on a wide-gamut monitor.' },
    ],
  },
  {
    slug: 'css-gradient-generator', title: 'CSS Gradient Generator', shortTitle: 'Gradient Generator', icon: '▨', category: 'Developer',
    description: 'Build linear and radial CSS gradients visually with multiple colour stops and copy the generated CSS.',
    tags: ['css gradient', 'linear gradient', 'radial', 'generator', 'background'],
    intro: 'Hand-writing a gradient means editing CSS, saving, and refreshing until the angle looks right. This generator gives you a live preview: set the type, angle, and colour stops, watch it update, then copy production-ready CSS with no vendor prefixes needed for any current browser.',
    steps: [
      'Choose a linear or radial gradient.',
      'Set the angle or radial shape.',
      'Add colour stops and drag their positions.',
      'Copy the generated CSS straight into your stylesheet.',
    ],
    faqs: [
      { q: 'How do gradient angles work in CSS?', a: '0deg points upward and angles increase clockwise, so 90deg runs left to right and 180deg runs top to bottom. This differs from the mathematical convention, which catches people out.' },
      { q: 'Why does my gradient look muddy in the middle?', a: 'Because interpolating in sRGB passes through a desaturated midpoint, most obviously between complementary colours. Adding an intermediate stop, or using an oklch interpolation hint in modern browsers, fixes it.' },
      { q: 'Do I still need vendor prefixes?', a: 'No. Unprefixed linear-gradient and radial-gradient have been supported by every current browser for many years. Prefixes only add weight.' },
      { q: 'Can a gradient be animated?', a: 'Not the gradient itself in a simple transition, since background-image does not interpolate. The usual trick is to animate background-position on an oversized gradient, or use CSS custom properties registered with @property.' },
    ],
  },
  {
    slug: 'box-shadow-generator', title: 'CSS Box Shadow Generator', shortTitle: 'Box Shadow', icon: '▢', category: 'Developer',
    description: 'Design CSS box shadows visually with offset, blur, spread, colour, and inset controls, then copy the CSS.',
    tags: ['box shadow', 'css', 'generator', 'shadow', 'elevation'],
    intro: 'Box shadow takes five values in a specific order and the difference between blur and spread is not obvious from the syntax alone. This generator gives you sliders and a live preview, plus the ability to layer several shadows — which is how convincing depth is actually built.',
    steps: [
      'Adjust horizontal and vertical offset to set the light direction.',
      'Raise blur for a soft edge and spread to grow or shrink the whole shadow.',
      'Pick a colour and opacity, or switch to inset for an inner shadow.',
      'Layer additional shadows for realistic depth, then copy the CSS.',
    ],
    faqs: [
      { q: 'What is the difference between blur and spread?', a: 'Blur softens the shadow edge without changing its size. Spread grows or shrinks the shadow shape before blurring, so a negative spread with a large blur produces a tight, subtle shadow.' },
      { q: 'How do I make a shadow look realistic?', a: 'Layer two or three. A tight, darker shadow close to the element plus a wide, faint one further out mimics real ambient and direct light far better than one heavy shadow.' },
      { q: 'When should I use inset?', a: 'For elements that should read as recessed — pressed buttons, input fields, and inset panels. It draws the shadow inside the border box instead of outside it.' },
      { q: 'Do box shadows hurt performance?', a: 'Large blur radii on many elements can cost paint time, especially while scrolling. If it becomes a problem, promote the element to its own layer or reduce the blur radius.' },
    ],
  },
  {
    slug: 'http-status-codes', title: 'HTTP Status Code Reference', shortTitle: 'HTTP Status Codes', icon: '200', category: 'Developer',
    description: 'Searchable reference of every HTTP status code with meaning, typical cause, and what to do about it.',
    tags: ['http status', '404', '301', '500', 'reference'],
    intro: 'HTTP status codes carry more meaning than most APIs use. The difference between a 301 and a 302 determines whether ranking signals transfer; the difference between 401 and 403 is authentication versus authorisation. This searchable reference covers every standard code with its practical implications.',
    steps: [
      'Search by code number or by keyword.',
      'Filter by class — 2xx success, 3xx redirect, 4xx client error, 5xx server error.',
      'Read the meaning and the typical cause.',
      'Check the SEO note on the redirect and error codes that affect indexing.',
    ],
    faqs: [
      { q: 'What is the difference between a 301 and a 302 redirect?', a: '301 is permanent and passes ranking signals to the new URL; 302 is temporary and tells search engines to keep the original indexed. Using 302 for a permanent move is a common and costly mistake.' },
      { q: 'When should I return 404 rather than 410?', a: '404 means not found, possibly temporarily. 410 means deliberately and permanently gone, and search engines typically drop a 410 URL from the index faster.' },
      { q: 'What is a soft 404?', a: 'A page that says "not found" to the user but returns HTTP 200. Search engines index it as a real page, which wastes crawl budget. Single-page applications produce these very easily.' },
      { q: 'What is the difference between 401 and 403?', a: '401 means you have not authenticated, so credentials might help. 403 means you are authenticated but not allowed, so retrying with the same identity will not.' },
    ],
  },
  {
    slug: 'meta-tag-generator', title: 'SEO Meta Tag Generator', shortTitle: 'Meta Tag Generator', icon: 'SEO', category: 'Developer',
    description: 'Generate title, description, canonical, Open Graph, and Twitter Card meta tags with live length validation and a SERP preview.',
    tags: ['meta tags', 'open graph', 'twitter card', 'seo', 'serp preview'],
    intro: 'Getting head tags right matters more than almost any other on-page factor, and getting them subtly wrong — a description truncated at 160 characters, a missing og:image, a canonical pointing at the wrong protocol — is invisible until traffic suffers. This generator builds the full set with live length warnings and a search result preview.',
    steps: [
      'Enter your page title, description, URL, and social image.',
      'Watch the length indicators — titles truncate around 60 characters, descriptions around 155.',
      'Check the search result and social card previews.',
      'Copy the complete block of meta tags into your page head.',
    ],
    faqs: [
      { q: 'How long should a title tag be?', a: 'Around 50 to 60 characters. Google truncates on pixel width rather than character count, so wide capitals eat the budget faster than narrow lowercase letters.' },
      { q: 'Does the meta description affect rankings?', a: 'Not directly as a ranking factor, but it heavily influences click-through rate from the results page, and Google will rewrite it if it judges yours a poor match for the query.' },
      { q: 'Why does my Facebook share show the wrong image?', a: 'Facebook caches Open Graph data aggressively. Update the tags, then run the URL through their sharing debugger to force a re-scrape.' },
      { q: 'When do I need a canonical tag?', a: 'Whenever the same content is reachable at more than one URL — tracking parameters, trailing slashes, http and https variants, or pagination. A self-referencing canonical on every page is a safe default.' },
    ],
  },
  {
    slug: 'robots-txt-generator', title: 'Robots.txt Generator', shortTitle: 'Robots.txt Generator', icon: 'BOT', category: 'Developer',
    description: 'Build a valid robots.txt file with crawl rules per user agent, disallowed paths, and a sitemap reference.',
    tags: ['robots.txt', 'crawler', 'disallow', 'sitemap', 'seo'],
    intro: 'A robots.txt file is four lines of text that can accidentally deindex an entire site, which is why generating it carefully beats copying one from a forum. This tool builds the file from a few choices and explains what each directive actually does — including the one thing robots.txt cannot do.',
    steps: [
      'Choose whether to allow all crawlers by default.',
      'Add the paths you want to block, such as admin or internal search.',
      'Add rules for specific user agents if you need to treat them differently.',
      'Add your sitemap URL, then copy the file to your site root.',
    ],
    faqs: [
      { q: 'Does robots.txt keep a page out of Google?', a: 'No, and this is the most damaging misunderstanding about it. It stops crawling, not indexing — a blocked URL with inbound links can still appear in results without a snippet. Use a noindex meta tag to prevent indexing.' },
      { q: 'Where must the file live?', a: 'At the root of the domain, exactly at /robots.txt. Crawlers do not look anywhere else, and it applies per protocol, subdomain, and port.' },
      { q: 'What happens if I disallow everything?', a: 'Crawlers stop fetching your pages, existing entries gradually decay out of the index, and organic traffic disappears. A stray "Disallow: /" left over from staging is a genuinely common outage.' },
      { q: 'Should I block CSS and JavaScript?', a: 'No. Google renders pages to evaluate them, so blocking assets means it sees a broken layout and may judge the page poorly, especially on mobile.' },
    ],
  },
  {
    slug: 'user-agent-parser', title: 'User Agent Parser', shortTitle: 'User Agent Parser', icon: 'UA', category: 'Developer',
    description: 'Parse a user agent string to identify the browser, engine, operating system, and device type.',
    tags: ['user agent', 'parser', 'browser detection', 'device', 'headers'],
    intro: 'User agent strings are an archaeological record of browser history — nearly every browser still claims to be Mozilla for compatibility reasons dating back to the 1990s. This parser extracts the browser, rendering engine, operating system, and device type from your own string or any one you paste in.',
    steps: [
      'Your browser’s own user agent is loaded automatically.',
      'Or paste a string from a server log or analytics export.',
      'Read the detected browser, version, engine, OS, and device type.',
      'Use the raw string view to check the individual tokens.',
    ],
    faqs: [
      { q: 'Why does every browser say "Mozilla/5.0"?', a: 'Historical compatibility. Servers in the 1990s sniffed for Mozilla before sending modern HTML, so every browser adopted the prefix and none has been able to drop it since.' },
      { q: 'Is user agent detection reliable?', a: 'Not really. Strings are trivially spoofed, browsers freeze parts of them for privacy, and Chrome is actively reducing their detail. Feature detection is the correct approach for anything functional.' },
      { q: 'What are Client Hints?', a: 'The modern replacement: the browser sends structured Sec-CH-UA headers, and the server requests extra detail only when it genuinely needs it. Far more privacy-friendly than a single fingerprintable string.' },
      { q: 'Why does Chrome mention Safari and Edge mention Chrome?', a: 'Because they share engine lineage and each added tokens to avoid being served degraded pages. The result is a string that names four browsers and identifies one.' },
    ],
  },
  {
    slug: 'password-generator', title: 'Strong Password Generator', shortTitle: 'Password Generator', icon: 'PW', category: 'Security',
    description: 'Generate strong random passwords with custom length and character sets using the browser’s cryptographic generator.',
    tags: ['password generator', 'random password', 'secure', 'strong password', 'crypto'],
    intro: 'A strong password is long and genuinely random, and humans are bad at both. This generator uses the browser’s cryptographic random source, guarantees at least one character from each selected set, and shows the resulting entropy so you can judge strength as a number rather than a colour bar.',
    steps: [
      'Set the length — 16 characters or more for anything that matters.',
      'Choose which character sets to include.',
      'Optionally exclude lookalike characters such as l, 1, I, O and 0.',
      'Generate, copy, and store it in a password manager.',
    ],
    faqs: [
      { q: 'How long should a password be?', a: 'At least 16 random characters for important accounts. Length beats complexity: a 20-character lowercase password has far more entropy than an 8-character one with symbols scattered through it.' },
      { q: 'Is generating a password in a browser safe?', a: 'The generation itself is, since it uses crypto.getRandomValues and never leaves your device. The risk is your own machine — generate on a device you trust and store the result in a password manager.' },
      { q: 'What does entropy mean here?', a: 'Bits of randomness. Each bit doubles the guessing effort, so 80 bits is roughly a trillion times harder to brute force than 40. Anything above 75 bits is comfortable for most threat models.' },
      { q: 'Should I change passwords regularly?', a: 'Current guidance from NIST says no — forced rotation pushes people toward predictable variations. Change a password when there is a reason to: a breach, a shared device, or any suspicion of exposure.' },
    ],
  },
  {
    slug: 'password-strength-checker', title: 'Password Strength Checker', shortTitle: 'Password Strength', icon: '★', category: 'Security',
    description: 'Test how strong a password is, see its entropy in bits, and get an estimate of how long it would take to crack.',
    tags: ['password strength', 'entropy', 'crack time', 'security', 'checker'],
    intro: 'Password strength meters usually reward the wrong things — a capital letter and an exclamation mark score well while the underlying word stays in every cracking dictionary. This checker calculates entropy from the actual character space, penalises common patterns, and estimates offline cracking time at realistic hardware speeds.',
    steps: [
      'Type a password, or something similar to one you use.',
      'Read the entropy in bits and the estimated time to crack offline.',
      'Check the warnings for common words, keyboard runs, and repeated characters.',
      'Lengthen the password until entropy comfortably exceeds 75 bits.',
    ],
    faqs: [
      { q: 'Is it safe to type my real password here?', a: 'Nothing is transmitted or stored — analysis runs entirely in your browser. Even so, the safest habit is to test a similar password rather than the exact one you use.' },
      { q: 'Why does "P@ssw0rd1" score badly?', a: 'Because the substitutions are the first thing every cracking tool tries. The dictionary word underneath is what matters, and rule-based attacks expand it in milliseconds.' },
      { q: 'How is crack time estimated?', a: 'From entropy divided by an assumed offline attack rate against a fast hash. It is a rough order of magnitude — a site using bcrypt is dramatically slower to attack than one using unsalted SHA-256.' },
      { q: 'Are passphrases better than passwords?', a: 'Yes, when the words are chosen randomly. Four or five random dictionary words give high entropy and are far easier to remember. Choosing a memorable phrase yourself removes most of that advantage.' },
    ],
  },
  {
    slug: 'credit-card-validator', title: 'Credit Card Number Validator', shortTitle: 'Card Validator', icon: '💳', category: 'Security',
    description: 'Check whether a card number passes the Luhn checksum and identify the card network from its prefix.',
    tags: ['luhn', 'credit card', 'validator', 'checksum', 'card type'],
    intro: 'Every payment card number carries a check digit calculated with the Luhn algorithm, which catches most single-digit typos and transpositions before a request ever reaches a payment processor. This tool runs that check and identifies the issuing network from the number prefix.',
    steps: [
      'Enter the card number; spaces and dashes are ignored.',
      'See whether the Luhn checksum passes.',
      'Read the detected card network and expected length.',
      'Use the generated test numbers for development rather than a real card.',
    ],
    faqs: [
      { q: 'Does passing Luhn mean the card is real?', a: 'No. It only means the digits are internally consistent. Whether the account exists, has funds, or is active can only be answered by the issuing bank.' },
      { q: 'How does the Luhn algorithm work?', a: 'Starting from the right, double every second digit, subtract 9 from any result above 9, sum everything including the untouched digits, and check that the total divides by 10.' },
      { q: 'Is it safe to enter a card number here?', a: 'The check runs entirely in your browser with no network request. Even so, use the published test numbers rather than a live card — good habit, and it is what the tool is for.' },
      { q: 'How is the card network identified?', a: 'From the leading digits: 4 for Visa, 51–55 and 2221–2720 for Mastercard, 34 and 37 for American Express, 6011 and 65 for Discover.' },
    ],
  },
  {
    slug: 'email-validator', title: 'Email Address Validator', shortTitle: 'Email Validator', icon: '@', category: 'Security',
    description: 'Check email address syntax in bulk, flag common typos in popular domains, and separate valid from invalid entries.',
    tags: ['email validator', 'syntax check', 'bulk', 'typo', 'list cleaning'],
    intro: 'Bad addresses in a mailing list hurt deliverability and waste sending quota, and most of them are simple typos — gmial.com, hotmial.com, a trailing comma from a spreadsheet. This validator checks syntax against the practical rules mail servers enforce and flags likely misspellings of common domains.',
    steps: [
      'Paste one address per line, or a comma-separated list.',
      'Review the split into valid and invalid entries.',
      'Check the suggested corrections for likely domain typos.',
      'Copy the cleaned list of valid addresses.',
    ],
    faqs: [
      { q: 'Does this confirm the mailbox exists?', a: 'No. Syntax validation cannot tell you whether an address receives mail — that requires an SMTP or API check against the receiving server. It does remove the addresses that could never work.' },
      { q: 'Why are plus signs allowed?', a: 'Because they are valid and widely used for sub-addressing, as in name+shop@example.com. Rejecting them is a common bug that blocks legitimate users.' },
      { q: 'Can email addresses contain non-English characters?', a: 'Yes, internationalised addresses are standardised, though support across providers is uneven. This tool accepts them rather than rejecting valid addresses outright.' },
      { q: 'Why does validation catch a typo like gmial.com?', a: 'It compares each domain against a list of very common providers using edit distance, so a one or two-character slip is flagged with the likely intended domain.' },
    ],
  },
];
